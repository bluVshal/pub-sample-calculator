import React from 'react';
import Assemble from '../Components/Assemble/Assemble';

const Calculator = () => {
    return(
        <div>
            <Assemble />
        </div>
    );
}

export default Calculator;